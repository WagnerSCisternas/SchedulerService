spring.application.name=schedulerService
# Configurações do Servidor SMTP (E-mail) para Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=devwasc@gmail.com
spring.mail.password=xzidhggtgechisrr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Configurações da sua API de Tarefas (mantidas como antes)
api.tasks.base-url=${API_TASKS_BASE_URL}
api.tasks.auth-url=${API_TASKS_AUTH_URL:/auth/login}
api.tasks.username=${API_TASKS_USERNAME}
api.tasks.password=${API_TASKS_PASSWORD}

# Configurações do Scheduler (mantidas como antes)
spring.task.scheduling.pool.size=5
spring.task.scheduling.thread-name-prefix=scheduler-task-